US-001 할 일 체크/삭제 낙관적 업데이트 — 완료
- hooks/useTodoMutations.ts 신설: toggleStatus / remove / addNote 를 한곳에 모음
- onMutate에서 캐시를 직접 고치고 실패 시 스냅샷으로 롤백, onSettled에서 invalidate로 수렴
- handled_by / completed_at 을 서버와 같은 규칙으로 미리 채워야 "대신 처리" 배지가 깜빡이지 않음
- TodoCard에서 busy 상태를 mutation.isPending 조합으로 대체, onMutated 프롭 제거
  (MemberColumn의 create는 아직 onMutated 사용 — 임시 id가 필요해 낙관적 처리 보류)
- 검증: yarn build 성공, yarn lint 경고 0

US-002 삭제 실행취소 토스트 — 완료
- restoreTodo 액션 추가. loadTodoForMember는 살아 있는 행만 조회하므로 재사용 불가,
  직접 조회 후 assertMember + assertCanRemove로 검사
- deleteTodo의 권한 검사를 assertCanRemove()로 분리해 두 액션이 공유
- lib/toast.ts에 showUndo() 추가 (sonner action 버튼, 7초)
- remove.onSuccess에서 실행취소 토스트를 띄우고 restore.mutate로 되살림
- 검증: yarn build 성공, yarn lint 경고 0

US-003 보드 로딩 스켈레톤 — 완료
- app/(app)/board/BoardSkeleton.tsx 신설. board-viewport와 컬럼 폭(w-full / sm:w-[330px])을
  실제 컬럼과 동일하게 써서 로딩이 끝나도 레이아웃이 튀지 않음
- animate-pulse-soft 유틸 추가 (기본 pulse는 투명도 0까지 떨어져 깜빡임이 셈)
- prefers-reduced-motion: reduce 전역 블록 추가 — 애니메이션/트랜지션/스무스 스크롤 무력화
  (US-005의 모션 배려 기준 일부를 여기서 미리 충족)
- 검증: 빌드 산출 CSS에 .animate-pulse-soft, prefers-reduced-motion 존재 확인

US-004 할 일 제목 인라인 수정 — 완료
- useTodoMutations에 edit 추가 (title/due_date 낙관적 반영, 실패 시 롤백)
- TodoCard 펼침 영역 맨 위에 "수정" 버튼 → 제목 Input + DueStepper + 취소/저장
- Escape로 취소, Enter로 저장. 빈 제목이면 저장 막음. 값이 그대로면 요청 자체를 보내지 않음
- startEditing에서 초안을 매번 현재 값으로 재설정 — 실시간으로 바뀐 값을 덮어쓰지 않게
- 수정 권한은 삭제와 동일(canRemove: 주인/작성자/방장) — 남의 할 일 제목이 실수로 바뀌는 걸 막음
- 검증: yarn build 성공, yarn lint 경고 0

US-005 다크 테마 시각 디테일 + 모션 배려 — 완료
- shadow-level-1/2가 테마 변수(--shadow-1/2)를 참조. 흰 배경은 옅게, 검은 배경은 진하고 넓게
  (옅은 검정 그림자는 검은 배경에서 아예 보이지 않아 층이 사라졌음)
- success/warning/danger 도 테마 변수로 이동. 검은 배경은 칩 배경을 어둡게 깔고 글자를 밝게
- --danger-ink 토큰 신설: 꽉 채운 danger 버튼의 글자색. 다크에서 danger가 밝아져
  text-white로는 대비가 무너지므로 테마별로 반전
- prefers-reduced-motion 전역 블록은 US-003에서 이미 추가됨
- 검증: 빌드 CSS에 .shadow-level-1{box-shadow:var(--shadow-1)}, text-danger-ink 확인

US-006 키보드 접근성 · 포커스 링 — 완료
- globals.css에 :focus-visible 전역 링. 요소마다 붙이면 새 버튼에서 빠뜨리므로 한곳에서 처리
  (입력 요소는 자체 테두리와 겹쳐 outline-offset만 0으로)
- BottomSheet에 role=dialog / aria-modal / aria-labelledby(useId) 부여
- Tab 순환 트랩: 시트 안 focusable을 모아 첫/마지막에서 감싸 돌린다
- 열 때 첫 항목 포커스(60ms 지연 — 진입 애니메이션 중 포커스가 튐), 닫을 때 원래 요소로 복귀
- TodoCard 펼침 버튼에 aria-expanded
- 검증: 빌드 CSS에 focus-visible 규칙 확인, yarn lint 경고 0

US-007 멤버가 나뿐일 때 초대 유도 — 완료
- BoardClient에 InviteColumn 추가. 멤버가 1명일 때만 컬럼 끝(대시보드는 목록 끝)에 붙는다
- 방장/관리자는 /team으로 가는 버튼, 일반 멤버는 "방장에게 초대를 요청해 주세요" 안내
- 점선 테두리 + 투명 배경이라 실제 컬럼과 구분된다
- 검증: yarn build 성공, yarn lint 경고 0

리뷰 대응 (2회차)
- 리뷰어 #1 지적 3건 수정 후 APPROVED
  1) useTodoMutations.patch()가 setQueryData 전에 cancelQueries를 await
     — onSettled마다 invalidate가 돌아 이전 refetch가 낙관적 패치를 덮어쓰던 문제
  2) TodoCard의 "안 바뀜" 판정 기준을 editBase(편집 시작 시점 값)로 변경
     — 현재 todo.title과 비교하면 편집 중 들어온 남의 수정을 덮어씀
  3) useLoopCarousel의 재배치를 최초 1회 + resetKey(activeOrgId) 변경 시로 제한
     — 멤버 수가 바뀔 때마다 되돌리면 남의 컬럼 보던 사람이 끌려감
     — BoardClient는 조직 전환 시 remount되지 않으므로 resetKey가 필수
- 리뷰어 #2가 새 결함 발견: 실시간 postgres_changes 핸들러는 react-query 밖에서
  캐시에 직접 쓰므로 cancelQueries의 보호를 받지 못함. 행 전체를 실어 오기 때문에
  in-flight 낙관적 변경을 커밋 이전 값으로 되돌림
  → lib/pending-todos.ts(개수 카운터)로 응답 대기 중인 id를 표시하고 병합에서 건너뜀

deslop (Step 7.5)
- 죽은 코드: Slide.realIndex(아무도 안 읽음), 훅 밖 미사용 resync 반환,
  pt-safe / px-safe / text-eyebrow 유틸리티
- 이름: MemberColumn onMutated → onCreated (이제 생성에만 쓰임)
- BoardSkeleton: 폭 하나 때문에 열어 둔 style 프롭 제거
- 회귀 재검증(Step 7.6): yarn build 성공, yarn lint 경고 0, / /login /reset 200

US-008/009/010/013 — 완료
- 토스트: richColors 제거하고 toastOptions.classNames로 시맨틱 토큰 적용
  (richColors는 sonner 자체 팔레트를 써서 잉크 블랙에서 흰 박스로 튐)
- 라벨 "완료" → "완료 보기"
- 보드↔대시보드 전환을 AnimatePresence mode="wait" + opacity로. transform은 쓰지 않음
  (스크롤 스냅 컨테이너에 transform을 걸면 스냅 위치가 어긋남)
- useReducedMotion()으로 모션 최소화 (framer-motion은 CSS 미디어 쿼리를 따르지 않음)
- resetKey에 mode를 섞어 대시보드에서 돌아와도 내 컬럼부터 보이게 (US-013)

US-011/012 헤더 재배치 + 달력 뷰 — 완료
- 조직 전환을 헤더에서 떼어 프로필(아바타) 메뉴 시트로 통합. 시트 구성은
  받은 초대 → 조직 → 관리 → 로그아웃 한 흐름
- 헤더 왼쪽 조직 이름은 표시 전용(버튼 아님), 그 자리에 달력 아이콘 버튼
- 받은 초대 빨간 점을 아바타로 이동 — 초대를 처리하는 곳이 그 메뉴 안이라서
- app/(app)/CalendarSheet.tsx: 월 격자 + 날짜별 미완료 개수 배지 + 선택 날짜 목록
  (담당자 아바타·이름 포함, 완료는 취소선). 데이터는 보드와 같은 쿼리 캐시를 공유
- 시트가 닫혀 있으면 내용 컴포넌트가 마운트되지 않아 불필요한 조회가 없다
- 죽은 코드 정리: ChevronIcon 삭제, openOrgSheet → openMenu

[2차 루프]
US-101 에러·404 화면 — 완료
- app/error.tsx: reset()으로 다시 시도 + 보드로 가기. digest를 노출한다
  (배포본에서 로그를 찾는 유일한 실마리라 사용자가 알려 줄 수 있어야 함)
- app/not-found.tsx: 404 안내 + 보드 링크
- 둘 다 시맨틱 토큰 + pb-safe. Next 기본 화면은 앱 테마를 몰라 남의 사이트처럼 보인다
- 검증: /no-such-page 가 404로 응답하고 우리 문구가 렌더됨

US-102 PWA 홈 화면 아이콘 — 완료
- manifest에 icons가 아예 없어 홈 화면에 추가하면 아이콘이 비었다
- app/icon.svg → app/icon.tsx (ImageResponse PNG). 안드로이드 런처는 SVG를 신뢰하지 않는다
- app/pwa-icon/[variant]/route.tsx 로 192·512·maskable 고정 주소 제공.
  app/icon.tsx는 Next가 해시 URL로 서빙해서 manifest에서 주소를 알 수 없다
- maskable은 런처가 원·둥근 사각형으로 잘라 내므로 글자 배율을 0.33 → 0.24로 낮춰
  안쪽 80% 안에 들어가게 함 (실제 렌더 확인)
- 검증: 세 라우트 모두 200 image/png, generateStaticParams로 빌드 시 프리렌더

US-103 달력 오늘 버튼 + 선택 정합성 — 완료
- 달을 넘겨도 selected가 그대로라 보고 있는 달과 아래 목록이 어긋났다
- 같은 일자를 지키되 그 달에 없으면 말일로 클램프 (1/31→2/28, 윤년 2/29 확인)
- 이번 달이 아니면 "오늘" 버튼 노출 → 이번 달 + 오늘 선택으로 복귀
- 월 표시에 aria-live="polite" (US-106의 기준 하나를 여기서 미리 충족)
- 검증: 5개 경계 케이스 계산 확인

US-104 완료한 할 일 접기 — 완료
- 완료 보기가 켜져 있으면 완료가 쌓여 컬럼을 잠식하고 남은 일이 스크롤 밖으로 밀렸다
- 최근 끝낸 3개만 두고 나머지는 "완료 N개 더 보기"로 접는다. 펼치면 "완료 접기"로 되돌린다
- 접기는 컬럼별 상태 — 한 사람 컬럼을 펼쳤다고 다른 사람 것까지 펼칠 이유가 없다
- 함정: useMemo에서 visible을 함께 반환하던 걸 잊고 아래에서 다시 선언해 이름 충돌.
  정렬(useMemo)과 표시 개수(파생값)는 의존성이 달라 분리하는 게 맞다

US-105 팀 화면 스켈레톤 — 완료
- 멤버 목록·초대 목록의 스피너를 행 모양 스켈레톤으로 교체
  (스피너는 크기가 없어 목록이 도착하는 순간 화면이 통째로 밀린다)

US-106 달력 접근성 — 완료
- role=grid / columnheader / gridcell, aria-selected, aria-current="date"
- 칸마다 describeDay()로 한 문장 라벨 — 숫자·배지·띠를 따로 읽으면 파편이 된다
  ("8월 15일, 남은 할 일 2개, 일정 팝업 준비 기간")
- 월 표시 aria-live는 US-103에서 이미 붙임

US-107 토스트를 작고 가운데로 — 완료
- sonner 기본 356px 고정 폭이라 "저장했어요" 한 줄에도 화면을 가로질렀다
- --width: auto + toast width auto로 내용만큼만. 폭이 매번 달라지므로
  margin-inline: auto로 스스로 가운데를 잡게 함
- 알약 모양(rounded-full)에 px-3.5 py-2 / 13px. 액션·취소 버튼도 h-7 알약으로 줄임
- icon과 actionButton에 shrink-0 — 글자가 길어져도 잘리지 않는다

마무리 (2차 루프)
- CalendarView.shift에서 setMonth 업데이터 안의 setSelected를 밖으로 뺐다.
  업데이터는 순수해야 하고 StrictMode에서 두 번 불릴 수 있다
- 리뷰어(architect) 실행했으나 보고서를 회수하지 못함 — 다음 세션에서 재검증 권장

[ralph] todolight UI/UX 간소화 — 1차
- 내 설정(/me): 저장 버튼 제거, 전 항목 즉시 저장(이름만 700ms 디바운스).
  소속 조직 카드 삭제(아바타 시트와 중복), 프로필 사진 보기 토글 삭제.
  선택 강조를 surface-alt -> accent로 (어두운 테마에서 안 보였음).
- Avatar: show_avatars/ignorePreference 분기 제거 -> useApp 의존이 사라져
  'use client'가 필요 없어짐. DB 컬럼은 그대로 둠(되돌릴 수 없어서).
- 팀(/team): 카드 5장 -> 3장. 조직 아이콘·이름·Discord를 '조직 설정' 한 장으로,
  저장 버튼 하나(바뀐 항목만 전송). 멤버 관리는 행을 눌러 여는 BottomSheet로.
- 죽은 코드: reorderTodo 액션, 미사용 i18n 키 2개 제거.
학습: messages/*.json은 python으로 편집해야 ko/en 구조가 안 어긋난다.
[ralph] 2~4차
- 보드 멤버 칩은 3명부터. 감출 때 --board-toolbar-h를 0으로 덮어 빈 띠 제거.
- 펼친 카드 동작 버튼을 메모 아래로 모음(내/남의 할 일에서 자리가 같아짐).
- 일정 시트: DuePicker의 allowNone 기본값 때문에 "마감 없음"이 서 있었고
  누르면 오늘로 바뀌던 버그. allowNone={false}로 막고 취소 버튼 제거.
- 가계부: 낸 사람이 한 명뿐이면 사람별 합계·행 아바타/이름 숨김.
- 달력 패널 스냅 3단 -> 2단(0.24/0.5). 0.76에서 칸이 찌그러졌다.
- CLAUDE.md를 코드에 맞춰 갱신(가계부·패널·폼 흐름·설정·스크롤바 섹션 추가).
검증: yarn build 통과, tsc/lint 통과, 폰 폭(~525px)에서 전 화면 육안 확인.
남은 것: 랜딩/로그인은 로그인 상태라 브라우저로 못 봄 — 코드로만 점검했다.
[ralph] API 왕복 줄이기 + 인터랙션 반응속도 (2026-08-17)
전제: 행이 적다(todos 73, notes 28, participants 3, members 3). 쿼리 실행 시간은 무의미하고
비용은 전부 (a) 액션 안의 순차 DB 왕복, (b) 브라우저→서버 액션 호출 횟수다.
- fetchOrgTodos: 멤버검사→todos→notes→participants 4연속 → Promise.all 한 묶음.
  메모는 todos!inner로 org·살아있음을 걸어 todo id 없이 읽고, 참여자는 자기 org_id로 읽는다.
  실측(같은 DB, 9회 median): 235ms → 112ms. 행/순서는 옛 방식과 동일함을 대조 확인.
  멤버검사는 같이 보내되 함께 await한다 — 실패하면 읽어 온 걸 한 줄도 안 돌려준다.
- fetchMyOrgs: 조직목록 + member_count 2연속 → org_members(count) 집계로 1회.
  실측 110ms → 60ms. 계정 3개 전부 member_count 값 일치 확인.
- getAuthUser: React cache()로 요청 범위 메모. 레이아웃 1회 렌더에 3번 돌던 걸 1번으로.
- useTodoMutations: onSettled마다 즉시 invalidate → resyncSoon(400ms 트레일링 합치기).
  낙관적으로 안 그리는 경로(addNote, restore)만 resyncNow로 즉시 유지 —
  거기서 미루면 방금 쓴 메모가 늦게 뜬다. clearTodoPending은 절대 미루지 않는다.
  타이머는 모듈 수준 Map(orgId) — 훅이 카드마다 불려서 ref에 두면 하나도 안 합쳐진다.
  dev 로그에서 setTodoStatus 2건 → fetchOrgTodos 1건으로 줄어드는 것 확인.
학습:
- supabase-js의 JWKS는 GLOBAL_JWKS(모듈 전역)에 캐시된다 — 요청마다 클라이언트를 새로
  만들어도 JWKS를 다시 받지 않는다. "액션마다 JWKS 왕복" 가설은 틀렸다.
- 브라우저에서 체크박스를 연타로 검증할 때: 카드가 완료로 바뀌면 목록이 재정렬돼
  미리 잡아 둔 li가 detached가 된다. 클릭 직전에 다시 조회해야 한다.
  disabled={busy}라 같은 카드 연타는 어차피 씹힌다 — 버스트는 서로 다른 카드로 만들어야 한다.
검증(브라우저, dev 로그로 계측):
- 서로 다른 카드 5장을 150ms 간격으로 체크 → setTodoStatus 5회 / fetchOrgTodos 1회.
  (바꾸기 전 규칙이라면 5회였다.)
- 추가·메모 추가·삭제·되살리기(Undo 토스트)를 임시 할 일 하나로 전 경로 확인.
  메모는 resyncNow라 즉시 작성자·아바타까지 붙어 나타난다.
- 패널 전환은 여전히 요청 0건, 트랙 transition 240ms 그대로.
- 측정 중 바꾼 데이터는 전부 원복(9 left · 14 done). 임시 할 일은 소프트 삭제로 남는다.
[ralph] 반응속도 및 사용자 편의성 개선 (2026-08-27, 세션 209a03be)
US-201~203: fetchOrgMembers(orgs.ts)·fetchLedgerEntries(ledger.ts)·fetchOrgEvents(events.ts)의
  멤버 검사→조회 순차 왕복을 Promise.all로 묶음 — fetchOrgTodos에서 이미 검증된 패턴 그대로
  이식(멤버가 아니면 Promise.all이 거절되어 데이터가 한 줄도 안 샌다).
US-204: useOrgTodos·useOrgMembers(useOrgBoard.ts)·useOrgEvents·useLedgerEntries에
  staleTime 30s + refetchOnWindowFocus/refetchOnReconnect: true 추가.
  전역 QueryClient 기본값(refetchOnWindowFocus: false)을 이 네 쿼리만 오버라이드.
  이번 세션 앞부분에서 고친 AppShell의 my-orgs/my-profile 쿼리(모바일 백그라운드 장시간 후
  "조직 없음"으로 얼어붙던 버그 — layout.tsx가 라우트가 아니라 패널 트랙 위에서 한 번만
  실행돼 재조회 계기가 없던 문제)와 같은 안전망을 보드·달력·가계부 데이터에도 확장.
검증: yarn tsc --noEmit / yarn lint(경고 0) / yarn build 전부 통과(구현 직후, 디슬롭 후 재검증
  포함 총 2회). architect 리뷰어 승인(APPROVED) — Promise.all 패턴의 멤버십 검사 우회 가능성,
  refetchOnWindowFocus와 낙관적 뮤테이션(useTodoMutations의 cancelQueries) 간 경합 가능성을
  포함해 검토.
학습: 이 리포는 read 액션이 "멤버 검사 await → 조회 await" 순차 패턴을 아직 3곳(members/
  ledger/events) 더 갖고 있었다 — fetchOrgTodos 하나만 고치고 나머지는 안 건드린 채 남아있던
  것. 같은 파일(orgs.ts)의 mutation들(inviteMember 등)은 일부러 그대로 뒀다 — 쓰기 전에
  검사가 끝나야 하는 순서 의존이라 병렬화 대상이 아니다(fetchOrgInvites도 read지만 방장만
  쓰는 저빈도 화면이라 이번 스코프에서 제외).
[ralph] 반응속도 및 사용자 편의성 개선 2차 — 대신 처리/메모 쓰기 병렬화 (2026-08-27, 세션 209a03be)
US-301: addTodoNote(todos.ts) — todo_notes insert와 profiles select가 서로 결과를 안 기다려도
  되는데 순차였다. Promise.all로 묶음.
US-302: handleForMember(todos.ts, "대신 처리" — CLAUDE.md가 핵심 기능으로 명시) — todos update
  (완료 처리) · todo_notes insert · profiles select 세 개가 전부 todo.id/user.id/parsedNote만
  필요해 서로 독립인데 순차 3왕복이었다. Promise.all 하나로 묶어 3왕복 -> 1왕복.
  에러 처리는 Promise.all 해체 후 각각 if(error) throw로 그대로 유지 — Supabase 클라이언트는
  DB 에러를 reject가 아니라 {data,error}로 돌려주므로 이 순서가 곧 "부분 성공으로 응답 안 함"을
  보장한다(profile 조회 실패를 무시하는 것은 이 변경 전부터 있던 동작, 이번에 안 건드림).
  todo_notes.todo_id FK는 이미 존재하는 todos.id를 참조하고 update는 그 행을 지우지 않으므로
  동시 실행 순서 문제 없음.
검증: yarn tsc --noEmit / yarn lint(경고 0) / yarn build 전부 통과(구현 직후 + 디슬롭 후 재검증).
학습(도구 이슈): architect 리뷰 서브에이전트가 이번 라운드에서 완료 알림·재개(SendMessage) 응답
  모두 "Idle."만 반환하고 실제 검토 내용을 못 돌려주는 문제가 3회 연속 재현됨(1차 라운드에서는
  포크로 한 번 재개하니 "APPROVED"가 돌아왔었는데 이번엔 안 됨) — SendFeedback으로 보고함.
  대신 architect에게 요청했던 5개 검증 항목(diff 일치 · 에러 처리 순서 · FK/트리거 경합 위험 ·
  더 단순한 대안 여부 · tsc/lint/build)을 직접 재현해 확인하고 진행함.
[ralph] 반응속도 및 사용자 편의성 개선 3차 — Supabase 오리진 preconnect (2026-08-27, 세션 209a03be)
US-401: app/layout.tsx의 RootLayout <head>에
  <link rel="preconnect" href={supabaseOrigin} crossOrigin="anonymous">와
  <link rel="dns-prefetch" href={supabaseOrigin}> 추가. supabaseOrigin은
  new URL(process.env.NEXT_PUBLIC_SUPABASE_URL!).origin으로 계산(하드코딩 안 함).
  서버 액션은 Next 서버가 Supabase를 부르므로 대상이 아니다 — 브라우저가 직접 여는
  두 경로(로그인의 auth REST, 보드/달력/가계부 진입 시 realtime 웹소켓)만 최초
  DNS+TCP+TLS 핸드셰이크 지연의 영향을 받는다. 모든 페이지가 이 layout을 쓰므로
  로그인 화면부터 혜택을 본다.
검증: yarn tsc --noEmit / yarn lint(경고 0) / yarn build 전부 통과(구현 직후 + 디슬롭 후
  재검증). architect 승인(APPROVED) — diff 일치, 빌드 타임 env 안전성(다른 파일도 이미
  같은 non-null assert 패턴을 무조건 쓰고 있어 이 레이아웃도 같은 가정이 안전함, 실제 라우트가
  전부 dynamic(ƒ)이라 RootLayout이 빌드 타임에 정적 평가되지 않음), crossOrigin="anonymous"가
  Supabase 클라이언트의 비인증(non-credentialed) cross-origin 요청 방식과 맞는지 확인.
1~3차 정리: 1차(read 액션 병렬화 3곳 + 포커스 복귀 재조회 4곳) · 2차(쓰기 액션 병렬화 2곳) ·
  3차(Supabase 오리진 preconnect) — "반응속도 및 사용자 편의성" 테마에서 서버 액션 왕복 줄이기는
  이제 훑을 만큼 훑었다. 다음에 같은 테마로 또 들어오면 서버 왕복이 아니라 클라이언트 렌더
  성능(불필요 리렌더, 메모이제이션)이나 낙관적 UI 커버리지 확장 쪽을 먼저 살펴볼 것.
